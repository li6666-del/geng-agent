# ADR-0003：共享卷产物存储并预留对象存储接口

## 状态

Accepted

## 决策

首期继续使用 case 目录作为产物真源，通过 `LocalArtifactStore` 统一做路径校验、索引、预览和导出。API 与 worker 在 Docker 中挂载同一数据卷。

## 后果

- 与 CLI 和历史案例完全兼容，不迁移大文件。
- 单文件、阶段 ZIP 和整案 ZIP 使用同一安全路径边界。
- 多主机部署前需实现同一接口的 S3/MinIO 后端。

## 安全边界

绝对路径、`..`、符号链接逃逸和隐藏临时文件均不能通过产物 API 暴露。
